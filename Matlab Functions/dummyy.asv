function done = dummyy(obj,event)
%UNTITLED2 Summary of this function goes here
%   Detailed explanation goes here
dataFromSerial=fscanf(obj);
dataFromSerial=strtrim(dataFromSerial)

   if(strncmpi(dataFromSerial, 'e', 1))
       if(strncmpi(dataFromSerial, '1', 3))
       end
       elseif(strncmpi(dataFromSerial, '2', 3))
   end
       ID=str2double(dataFromSerial(1,2));
       SetRobotData(dataFromSerial,ID);
   else
       SetRobotData(dataFromSerial,5);
   end 

end